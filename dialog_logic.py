"""
Логика многоэтапного диалога для бота GLADIS
"""

import re
from typing import Dict, Any

def analyze_client_needs(message: str, session: Dict[str, Any]) -> str:
    """
    Простой анализ. Основную работу делает AI.
    """
    message_lower = message.lower()
    
    # Приветственные фразы
    greetings = [
        "добрый день", "добрый вечер", "доброе утро",
        "здравствуйте", "привет", "доброго времени суток",
        "здрасьте", "приветствую"
    ]
    
    # Если это просто приветствие
    if any(greeting in message_lower for greeting in greetings):
        return "Здравствуйте! Клиника GLADIS, меня зовут Александра.\n\nЧем могу вам помочь?"
    
    # Только ОЧЕНЬ очевидные случаи обрабатываем здесь
    obvious_keywords = {
        "эпиляция": "Лазерная эпиляция",
        "лазер": "Лазерная эпиляция", 
        "чистка лица": "Чистка лица",
        "ботокс": "Ботулотоксин",
        "лифтинг": "Лифтинг",
        "пилинг": "Пилинг"
    }
    
    # Ищем очевидные ключевые слова
    for keyword, procedure in obvious_keywords.items():
        if keyword in message_lower:
            session['procedure_category'] = procedure
            
            # Для эпиляции - специальный ответ
            if procedure == "Лазерная эпиляция":
                response = f"Здравствуйте! Клиника GLADIS, меня зовут Александра.\n\n"
                response += f"Отлично! Лазерная эпиляция - это долговременное удаление волос.\n\n"
                response += f"У нас два типа лазеров:\n"
                response += f"1. Гибридный лазер Innovation - мягкий, курс 8-12 процедур\n"
                response += f"2. Александритовый лазер Quanta System - мощный, курс 5-7 процедур\n\n"
                response += f"Какие зоны вас интересуют? (подмышки, бикини, ноги, лицо и т.д.)"
                session['stage'] = 'details_clarification'
                return response
    
    # Если не нашли очевидного - возвращаем приглашение к диалогу
    response = "Здравствуйте! Клиника GLADIS, меня зовут Александра.\n\n"
    response += "Чем могу вам помочь? Расскажите, что вас интересует, или задайте вопрос."
    
    return response

def clarify_procedure_details(message: str, session: Dict[str, Any]) -> str:
    """
    Уточнение деталей процедуры (только для эпиляции).
    """
    message_lower = message.lower()
    
    # Сохраняем ответы клиента
    if 'questions_answered' not in session:
        session['questions_answered'] = []
    session['questions_answered'].append(message)
    
    # Определяем зоны для эпиляции
    zones_keywords = {
        "подмышек": "подмышечные впадины",
        "бикини": "зона бикини",
        "голени": "голени",
        "ноги": "ноги полностью",
        "лицо": "лицо",
        "верхняя губа": "над губой",
        "подбородок": "подбородок"
    }
    
    for keyword, zone in zones_keywords.items():
        if keyword in message_lower:
            session['zone'] = zone
            break
    
    # Определяем тип лазера
    if "гибрид" in message_lower or "innovation" in message_lower:
        session['laser_type'] = "гибридный"
    elif "александрит" in message_lower or "quanta" in message_lower:
        session['laser_type'] = "александритовый"
    
    # Определяем местоположение
    if "сочи" in message_lower:
        session['location'] = "Сочи"
    elif "адлер" in message_lower:
        session['location'] = "Адлер"
    
    # Проверяем, достаточно ли информации
    questions_answered = len(session.get('questions_answered', []))
    
    if questions_answered < 2:
        if questions_answered == 0:
            return "Какие зоны интересуют? (подмышки, бикини, ноги, лицо и т.д.)"
        elif questions_answered == 1:
            return "Какой у вас тип кожи и цвет волос? (светлые/темные, тонкие/грубые)"
    else:
        session['stage'] = 'contact_collection'
        return prepare_epilation_summary(session)
    
    return "Расскажите еще о ваших ожиданиях от процедуры?"

def prepare_epilation_summary(session: Dict[str, Any]) -> str:
    """Подготовка сводки по эпиляции."""
    summary = "✅ Отлично! На основе ваших ответов:\n\n"
    
    if session.get('zone'):
        summary += f"📍 Зона: {session['zone']}\n"
        
        # Добавляем примерные цены
        if "подмышечные" in session['zone'].lower() or "подмышек" in session['zone'].lower():
            summary += "💰 Примерная стоимость:\n"
            summary += "   • Гибридный лазер: 1100 руб\n"
            summary += "   • Александритовый: 1400 руб\n"
    
    if session.get('laser_type'):
        laser_info = {
            "гибридный": "Laser Innovation (гибридный) - более мягкий, больше сеансов (8-12)",
            "александритовый": "Laser Quanta System (александритовый) - более мощный, меньше сеансов (5-7)"
        }
        summary += f"💡 Рекомендуемый лазер: {laser_info.get(session['laser_type'], session['laser_type'])}\n"
    
    if session.get('location'):
        summary += f"🏥 Клиника: {session['location']}\n"
        if session['location'] == "Адлер":
            summary += "   (В Адлере доступен только гибридный лазер)\n"
    
    summary += "\nДля записи мне нужно ваше имя и телефон."
    
    return summary

def should_move_to_contacts(message: str, session: Dict[str, Any]) -> bool:
    """
    Определяет, пора ли переходить к сбору контактов.
    """
    message_lower = message.lower()
    
    # Ключевые слова, указывающие на готовность записаться
    ready_keywords = [
        "хочу записаться", "запишите", "можно записаться", 
        "готов записаться", "давайте запишем", "хочу на процедуру",
        "интересует запись", "хочу сделать", "запишите меня",
        "давайте", "согласен", "ок", "хорошо", "идемте", "хотел записаться"
    ]
    
    # Если клиент явно говорит о записи
    if any(keyword in message_lower for keyword in ready_keywords):
        return True
    
    # Если клиент дает контакты
    contact_patterns = [
        r'\d{10,11}',
        r'[\+7]?[-\s]?\(?\d{3}\)?[-\s]?\d{3}[-\s]?\d{2}[-\s]?\d{2,3}',
        r'меня\s+зовут',
        r'имя\s+',
        r'телефон'
    ]
    
    for pattern in contact_patterns:
        if re.search(pattern, message_lower):
            return True
    
    # Если уже было много сообщений в диалоге
    if session.get('message_count', 0) >= 5:
        return True
    
    return False

def handle_contact_collection(message: str, session: Dict[str, Any]) -> str:
    """
    Этап 4: Сбор контактов.
    """
    print(f"🔍 Сбор контактов из сообщения: '{message}'")
    
    # Ищем телефон (улучшенный паттерн)
    phone_pattern = r'[\+7]?[-\s]?\(?\d{3}\)?[-\s]?\d{3}[-\s]?\d{2}[-\s]?\d{2,3}'
    phone_matches = re.findall(phone_pattern, message)
    
    # Также ищем просто 11 цифр подряд
    if not phone_matches:
        phone_pattern2 = r'\b\d{10,11}\b'
        phone_matches = re.findall(phone_pattern2, message)
    
    # Удаляем нецифровые символы из телефона
    if phone_matches:
        clean_phone = re.sub(r'\D', '', phone_matches[0])
        if 10 <= len(clean_phone) <= 11:
            session['phone'] = clean_phone
            print(f"📞 Найден телефон: {session['phone']}")
    
    # Ищем имя - УПРОЩЕННАЯ ЛОГИКА
    # Разбиваем сообщение на слова
    words = re.findall(r'[А-ЯЁа-яёA-Za-z]+', message)
    
    # Ищем русские слова с заглавной буквы (имена)
    russian_words = [word for word in words if re.match(r'^[А-ЯЁ][а-яё]*$', word)]
    
    if russian_words and not session['name']:
        # Берем первое слово с заглавной буквы как имя
        session['name'] = russian_words[0]
        print(f"👤 Найдено имя: {session['name']}")
    
    # Если имя не найдено, проверяем предыдущие сообщения
    if not session['name'] and len(session.get('text_parts', [])) > 0:
        # Проверяем последние 2 сообщения
        for prev_msg in session['text_parts'][-2:]:
            prev_words = re.findall(r'[А-ЯЁа-яёA-Za-z]+', prev_msg)
            prev_russian_words = [word for word in prev_words if re.match(r'^[А-ЯЁ][а-яё]*$', word)]
            if prev_russian_words:
                session['name'] = prev_russian_words[0]
                print(f"👤 Найдено имя из предыдущего сообщения: {session['name']}")
                break
    
    # Формируем ответ в зависимости от того, что уже есть
    has_name = bool(session['name'])
    has_phone = bool(session['phone'])
    
    if has_name and has_phone:
        return "Спасибо! Сейчас передам всю информацию администратору."
    elif has_name and not has_phone:
        return f"Спасибо, {session['name']}! Теперь укажите ваш телефон для связи."
    elif has_phone and not has_name:
        return f"Спасибо! Вижу ваш телефон {session['phone']}. Как вас зовут?"
    else:
        # Если ничего нет, просим оба
        return "Для записи мне нужно ваше имя и телефон для связи. Укажите их, пожалуйста."
