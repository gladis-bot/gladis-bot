"""
Логика многоэтапного диалога для бота GLADIS
"""

import re
from typing import Dict, Any

def analyze_client_needs(message: str, session: Dict[str, Any]) -> str:
    """
    Этап 1: Анализ потребностей клиента.
    Определяет, какая процедура интересует и начинает консультацию.
    """
    message_lower = message.lower()
    
    # Словарь процедур и их описаний
    procedures_info = {
        "пилинг": {
            "name": "Пилинг",
            "description": "Химическое обновление кожи",
            "types": [
                "Peach peel - 4500 руб (омолаживающий)",
                "Peel RDX, 07, 22 - 2000 руб",
                "PRX-TZZ - 5000 руб",
                "Biorepeel PQGE - 4500 руб",
                "Triple V45 - 4500 руб",
                "Карбоновый - 2000 руб (для проблемной кожи)",
                "Пировиноградный - 2500 руб",
                "Ретиноловый - 2000 руб",
                "Пилинг для глаз - 6000 руб"
            ],
            "questions": [
                "Какой тип кожи у вас? (жирная, сухая, комбинированная, проблемная)",
                "Какой результат хотите получить? (омоложение, лечение акне, осветление)"
            ]
        },
        "чистка": {
            "name": "Чистка лица",
            "description": "Аппаратная или механическая чистка",
            "types": [
                "Ультразвуковая - 3900 руб (бережная чистка)",
                "Механическая - 3900 руб (глубокая чистка)",
                "Комбинированная - 3900 руб",
                "Hydrafacial гидропилинг - 4500 руб (вакуумная чистка)",
                "Карбоновый пилинг+УЗ - 3350 руб",
                "Удаление миллиумов - 500 руб"
            ],
            "questions": [
                "Какое состояние кожи? (акне, расширенные поры, черные точки)",
                "Были ли у вас чистки раньше?"
            ]
        },
        "эпиляция": {
            "name": "Лазерная эпиляция",
            "description": "Удаление волос лазером",
            "types": [
                "Гибридный лазер Innovation",
                "Александритовый лазер Quanta System"
            ],
            "questions": [
                "Какие зоны интересуют?",
                "Какой у вас тип кожи и цвет волос?",
                "Делали ли лазерную эпиляцию раньше?"
            ]
        },
        "ботокс": {
            "name": "Ботулотоксин",
            "description": "Устранение мимических морщин",
            "types": [
                "Релатокс - 250 руб/ед.",
                "Ботулакс - 250 руб/ед.",
                "Гипергидроз (2 шт) - 20000 руб"
            ],
            "questions": [
                "Какие зоны интересуют? (лоб, межбровье, гусиные лапки)",
                "Делали ли инъекции ботулотоксина раньше?"
            ]
        },
        "лифтинг": {
            "name": "Лифтинг",
            "description": "Подтяжка кожи",
            "types": [
                "Микроигольчатый RF-лифтинг - от 16500 руб",
                "SMAS-лифтинг - от 14000 руб"
            ],
            "questions": [
                "Какие зоны интересуют? (лицо, шея, декольте)",
                "Какой возраст кожи?"
            ]
        }
    }
    
    # Определяем, о какой процедуре спрашивает клиент
    found_procedure = None
    for proc_key, proc_info in procedures_info.items():
        if proc_key in message_lower:
            found_procedure = proc_key
            session['procedure_category'] = proc_info['name']
            break
    
    # Если не нашли явно - ищем по ключевым словам
    if not found_procedure:
        keywords_mapping = {
            "омоложение": "пилинг",
            "акне": "чистка",
            "прыщ": "чистка",
            "волос": "эпиляция",
            "морщин": "ботокс",
            "подтяж": "лифтинг",
            "smas": "лифтинг",
            "ревиксан": "чистка"
        }
        
        for keyword, procedure in keywords_mapping.items():
            if keyword in message_lower:
                found_procedure = procedure
                session['procedure_category'] = procedures_info[procedure]['name']
                break
    
    # Формируем ответ
    if found_procedure:
        proc_info = procedures_info[found_procedure]
        
        response = f"Отлично! {proc_info['description']}.\n\n"
        response += f"📋 У нас есть следующие варианты {proc_info['name'].lower()}:\n"
        
        for i, proc_type in enumerate(proc_info['types'], 1):
            response += f"{i}. {proc_type}\n"
        
        response += "\n"
        
        # Добавляем вопросы для уточнения
        if proc_info['questions']:
            response += "Чтобы подобрать оптимальный вариант, ответьте на несколько вопросов:\n"
            for i, question in enumerate(proc_info['questions'], 1):
                response += f"{i}. {question}\n"
            session['stage'] = 'details_clarification'
        else:
            response += "Какой именно вариант вас интересует?\n"
            session['stage'] = 'consultation'
        
        response += "\nИли просто скажите, какой именно процедурой интересуетесь."
        
    else:
        # Если не поняли процедуру
        response = "Расскажите подробнее, что вас интересует?\n\n"
        response += "Например:\n"
        response += "- Чистка или пилинг для лица\n"
        response += "- Лазерная эпиляция\n"
        response += "- Ботулотоксин от морщин\n"
        response += "- Лифтинг или подтяжка кожи\n"
        response += "- Консультация косметолога\n\n"
        response += "Или задайте любой вопрос о процедурах!"
    
    # Всегда добавляем контакты
    response += "\n\n📞 Телефон для консультации: 8-928-458-32-88"
    response += "\n📍 Адрес: Сочи, ул. Воровского, 22. Ежедневно 10:00-20:00"
    
    return response

def clarify_procedure_details(message: str, session: Dict[str, Any]) -> str:
    """
    Этап 3: Уточнение деталей процедуры.
    """
    # Сохраняем ответы клиента
    if 'questions_answered' not in session:
        session['questions_answered'] = []
    
    session['questions_answered'].append(message)
    
    # В зависимости от процедуры задаем уточняющие вопросы
    procedure = session.get('procedure_category', '').lower()
    
    if 'эпиляция' in procedure:
        return handle_epilation_details(message, session)
    elif 'чистка' in procedure or 'пилинг' in procedure:
        return handle_skin_procedure_details(message, session)
    elif 'ботокс' in procedure:
        return handle_botox_details(message, session)
    elif 'лифтинг' in procedure:
        return handle_lifting_details(message, session)
    else:
        # Общий случай
        if len(session['questions_answered']) >= 2:
            session['stage'] = 'contact_collection'
            return "Спасибо за информацию! Теперь я могу порекомендовать оптимальный вариант.\n\nДля записи мне нужно ваше имя и телефон. Укажите их, пожалуйста."
        else:
            return "Расскажите еще о ваших ожиданиях от процедуры?"

def handle_epilation_details(message: str, session: Dict[str, Any]) -> str:
    """Уточнение деталей для эпиляции."""
    message_lower = message.lower()
    
    # Определяем зоны
    zones_keywords = {
        "подмышки": "подмышечные впадины",
        "бикини": "зона бикини",
        "голени": "голени",
        "ноги": "ноги полностью",
        "лицо": "лицо",
        "верхняя губа": "над губой",
        "подбородок": "подбородок"
    }
    
    for keyword, zone in zones_keywords.items():
        if keyword in message_lower:
            session['zone'] = zone
            break
    
    # Определяем тип лазера
    if "гибрид" in message_lower or "innovation" in message_lower:
        session['laser_type'] = "гибридный"
    elif "александрит" in message_lower or "quanta" in message_lower:
        session['laser_type'] = "александритовый"
    
    # Определяем местоположение
    if "сочи" in message_lower:
        session['location'] = "Сочи"
    elif "адлер" in message_lower:
        session['location'] = "Адлер"
    
    # Проверяем, достаточно ли информации
    questions_answered = len(session.get('questions_answered', []))
    
    if questions_answered == 1:
        return "Какой у вас тип кожи и цвет волос? (светлые/темные, тонкие/грубые)"
    elif questions_answered == 2:
        return "Делали ли лазерную эпиляцию раньше? Если да, то на каком аппарате?"
    elif questions_answered >= 3:
        session['stage'] = 'contact_collection'
        return prepare_epilation_summary(session)

def prepare_epilation_summary(session: Dict[str, Any]) -> str:
    """Подготовка сводки по эпиляции."""
    summary = "✅ Отлично! На основе ваших ответов:\n\n"
    
    if session.get('zone'):
        summary += f"📍 Зона: {session['zone']}\n"
    
    if session.get('laser_type'):
        laser_info = {
            "гибридный": "Laser Innovation (гибридный) - более мягкий, больше сеансов",
            "александритовый": "Laser Quanta System (александритовый) - более мощный, меньше сеансов"
        }
        summary += f"💡 Рекомендуемый лазер: {laser_info.get(session['laser_type'], session['laser_type'])}\n"
    
    if session.get('location'):
        summary += f"🏥 Клиника: {session['location']}\n"
        if session['location'] == "Адлер":
            summary += "   (В Адлере доступен только гибридный лазер)\n"
    
    summary += "\nДля записи мне нужно ваше имя и телефон."
    
    return summary

def handle_skin_procedure_details(message: str, session: Dict[str, Any]) -> str:
    """Уточнение деталей для процедур по уходу за кожей."""
    message_lower = message.lower()
    
    # Определяем тип кожи
    skin_types = ["жирная", "сухая", "комбинированная", "проблемная", "чувствительная"]
    for skin_type in skin_types:
        if skin_type in message_lower:
            session['skin_type'] = skin_type
            break
    
    # Определяем проблемы
    problems = ["акне", "прыщи", "расширенные поры", "черные точки", "пигментация", "купероз", "морщины"]
    found_problems = []
    for problem in problems:
        if problem in message_lower:
            found_problems.append(problem)
    
    if found_problems:
        session['skin_problems'] = found_problems
    
    questions_answered = len(session.get('questions_answered', []))
    
    if questions_answered == 1:
        return "Какой результат хотите получить? (чистая кожа, омоложение, лечение акне, осветление)"
    elif questions_answered == 2:
        return "Были ли у вас подобные процедуры раньше? Если да, то какие?"
    elif questions_answered >= 3:
        session['stage'] = 'contact_collection'
        return prepare_skin_procedure_summary(session)

def prepare_skin_procedure_summary(session: Dict[str, Any]) -> str:
    """Подготовка сводки по процедурам для кожи."""
    summary = "✅ Поняла вашу ситуацию! Рекомендации:\n\n"
    
    if session.get('skin_type'):
        summary += f"📝 Тип кожи: {session['skin_type']}\n"
    
    if session.get('skin_problems'):
        summary += f"🔍 Проблемы: {', '.join(session['skin_problems'])}\n"
        
        # Рекомендации на основе проблем
        recommendations = {
            "акне": "Рекомендую комплексное лечение с фотодинамической терапией",
            "пигментация": "Идеально подойдет фотоомоложение Lumeca",
            "купероз": "Лучше всего лазерное удаление сосудов",
            "морщины": "Рассмотрите микроигольчатый RF-лифтинг или ботулотоксин"
        }
        
        for problem, rec in recommendations.items():
            if problem in session['skin_problems']:
                summary += f"💡 {rec}\n"
                break
    
    summary += "\nДля записи на консультацию с косметологом мне нужно ваше имя и телефон."
    
    return summary

def handle_botox_details(message: str, session: Dict[str, Any]) -> str:
    """Уточнение деталей для ботокса."""
    message_lower = message.lower()
    
    # Определяем зоны
    zones = ["лоб", "межбровье", "гусиные лапки", "шея", "подбородок"]
    found_zones = []
    for zone in zones:
        if zone in message_lower:
            found_zones.append(zone)
    
    if found_zones:
        session['zones'] = found_zones
    
    questions_answered = len(session.get('questions_answered', []))
    
    if questions_answered == 1:
        return "Делали ли инъекции ботулотоксина раньше? Если да, то как давно?"
    elif questions_answered >= 2:
        session['stage'] = 'contact_collection'
        
        summary = "✅ Отлично! На основе ваших ответов:\n\n"
        if session.get('zones'):
            summary += f"📍 Зоны: {', '.join(session['zones'])}\n"
            summary += f"💉 Примерное количество единиц: {len(session['zones']) * 15-25} ед.\n"
            summary += f"💰 Ориентировочная стоимость: {len(session['zones']) * 15 * 250} - {len(session['zones']) * 25 * 250} руб.\n"
        
        summary += "\nТочную стоимость определит врач на консультации.\n"
        summary += "Для записи мне нужно ваше имя и телефон."
        
        return summary

def handle_lifting_details(message: str, session: Dict[str, Any]) -> str:
    """Уточнение деталей для лифтинга."""
    message_lower = message.lower()
    
    # Определяем зоны
    zones = ["лицо", "шея", "декольте", "руки", "живот", "бедра"]
    found_zones = []
    for zone in zones:
        if zone in message_lower:
            found_zones.append(zone)
    
    if found_zones:
        session['zones'] = found_zones
    
    questions_answered = len(session.get('questions_answered', []))
    
    if questions_answered == 1:
        return "Какой возраст кожи и насколько выражены возрастные изменения?"
    elif questions_answered >= 2:
        session['stage'] = 'contact_collection'
        
        summary = "✅ Поняла! Рекомендации:\n\n"
        
        if session.get('zones'):
            summary += f"📍 Зоны: {', '.join(session['zones'])}\n"
            
            # Рекомендации по процедурам
            if "лицо" in session['zones'] or "шея" in session['zones']:
                summary += "💡 Для лица и шеи идеально подойдет:\n"
                summary += "   - SMAS-лифтинг от 14000 руб\n"
                summary += "   - Микроигольчатый RF-лифтинг от 16500 руб\n"
                summary += "   - Комплекс с экзосомами и ФДТ от 60000 руб\n"
        
        summary += "\nДля подбора оптимальной процедуры нужна консультация.\n"
        summary += "Для записи мне нужно ваше имя и телефон."
        
        return summary

def should_move_to_contacts(message: str, session: Dict[str, Any]) -> bool:
    """
    Определяет, пора ли переходить к сбору контактов.
    """
    message_lower = message.lower()
    
    # Ключевые слова, указывающие на готовность записаться
    ready_keywords = [
        "хочу записаться", "запишите", "можно записаться", 
        "готов записаться", "давайте запишем", "хочу на процедуру",
        "интересует запись", "хочу сделать"
    ]
    
    # Если клиент явно говорит о записи
    if any(keyword in message_lower for keyword in ready_keywords):
        return True
    
    # Если уже было много сообщений в диалоге
    if session.get('message_count', 0) >= 5:
        return True
    
    return False

def handle_contact_collection(message: str, session: Dict[str, Any]) -> str:
    """
    Этап 4: Сбор контактов.
    """
    message_lower = message.lower()
    
    # Проверяем, есть ли в сообщении имя
    name_patterns = [
        r'меня\s+зовут\s+([А-ЯЁ][а-яё]+(?:\s+[А-ЯЁ][а-яё]+)?)',
        r'имя\s+([А-ЯЁ][а-яё]+(?:\s+[А-ЯЁ][а-яё]+)?)',
        r'([А-ЯЁ][а-яё]+(?:\s+[А-ЯЁ][а-яё]+)?)\s+(?:это|мое имя)',
        r'зовут\s+([А-ЯЁ][а-яё]+(?:\s+[А-ЯЁ][а-яё]+)?)',
    ]
    
    found_name = None
    for pattern in name_patterns:
        match = re.search(pattern, message, re.IGNORECASE)
        if match:
            found_name = match.group(1)
            break
    
    # Проверяем, есть ли в сообщении телефон
    phone_pattern = r'[\+7]?[-\s]?\(?\d{3}\)?[-\s]?\d{3}[-\s]?\d{2}[-\s]?\d{2}'
    phone_matches = re.findall(phone_pattern, message)
    
    # Обновляем данные
    if found_name and not session['name']:
        session['name'] = found_name
    
    if phone_matches and not session['phone']:
        session['phone'] = phone_matches[0]
    
    # Формируем ответ в зависимости от того, что уже есть
    has_name = bool(session['name'])
    has_phone = bool(session['phone'])
    
    if has_name and has_phone:
        return "Спасибо! Сейчас передам всю информацию администратору."
    elif has_name and not has_phone:
        return f"Спасибо, {session['name']}! Теперь укажите ваш телефон для связи."
    elif has_phone and not has_name:
        return f"Спасибо за телефон! Теперь скажите, как вас зовут?"
    else:
        # Если ничего нет, просим оба
        return "Для записи мне нужно ваше имя и телефон для связи. Укажите их, пожалуйста."
