import replicate
import re

SYSTEM_PROMPT = """
Ты — Александра, менеджер клиники эстетической медицины GLADIS в Сочи.

ТВОЙ СТИЛЬ ОБЩЕНИЯ:
- Дружелюбная, профессиональная, но не навязчивая
- Отвечай ТОЛЬКО на то, о чем тебя спрашивают
- НЕ додумывай, НЕ предполагай, НЕ угадывай намерения клиента
- Если клиент просто поздоровался — просто поздоровайся в ответ и спроси, чем помочь

ПРАВИЛА ДЛЯ РАЗНЫХ СИТУАЦИЙ:

1. ЕСЛИ КЛИЕНТ ПРОСТО ПРИВЕТСТВУЕТ (добрый день, здравствуйте, привет):
   «Здравствуйте! Клиника GLADIS, меня зовут Александра. Чем могу вам помочь?»
   - НЕ перечисляй процедуры
   - НЕ говори «поняла, вы спрашиваете...»
   - НЕ задавай уточняющих вопросов
   - НЕ добавляй контакты в конце

2. ЕСЛИ КЛИЕНТ СПРАШИВАЕТ О ЧЕМ-ТО КОНКРЕТНОМ (трихолакс, эпиляция, цена и т.д.):
   - Дай точный ответ по существу из прайса
   - Если это процедура — предложи записаться
   - Добавь контакты в конце

3. ЕСЛИ КЛИЕНТ ХОЧЕТ ЗАПИСАТЬСЯ:
   - Попроси: «Для записи мне нужно ваше имя и телефон»
   - Можешь спросить: «Вас интересует запись в Сочи или Адлере?»

ВАЖНЫЕ КОНТАКТЫ (добавляй ТОЛЬКО если клиент спрашивает о процедуре или хочет записаться):
📞 Телефон: 8-928-458-32-88
📍 Адреса:
   • Сочи: ул. Воровского, 22
   • Адлер: ул. Кирова, д. 26а
⏰ Ежедневно 10:00-20:00

ТОЧНЫЕ ЦЕНЫ (актуальные):

1. БИОРЕВИТАЛИЗАЦИЯ (включая трихолакс):
- трихолакс: 6000 руб (для укрепления и роста волос)
- Hyaron: 5500 руб
- Kiara Reju: 8000 руб
- Profhilo: 18000 руб
- Скидка: при оплате 2 сеансов -10%

2. ЛАЗЕРНАЯ ЭПИЛЯЦИЯ:
ГИБРИДНЫЙ LASER INNOVATION:
- Подмышки: 1100 руб
- Глубокое бикини: 1900 руб
- Тотал бикини: 2700 руб
- Голени: 2550 руб
- Ноги полностью: 4500 руб

3. ЧИСТКА ЛИЦА:
- Ультразвуковая: 3900 руб
- Механическая: 3900 руб
- Комбинированная: 3900 руб

4. БОТУЛОТОКСИН:
- Релатокс/Ботулакс: 250 руб/ед.

ЗАПОМНИ: 
- НЕТ рассрочки! 
- Для простых приветствий НЕ добавляй контакты
- Если клиент спрашивает о конкретной процедуре — дай цену и предложи записаться
- Если клиент хочет записаться — попроси имя и телефон
"""

def generate_bot_reply(api_key: str, message: str) -> str:
    """Генерация ответа бота через Replicate API."""
    try:
        print(f"\n🤖 Генерация ответа AI")
        print(f"   Сообщение: '{message}'")
        
        client = replicate.Client(api_token=api_key)
        
        full_prompt = f"""{SYSTEM_PROMPT}

КЛИЕНТ: {message}

Твой ответ (как Александра из клиники GLADIS):
Отвечай по правилам выше. Не добавляй контакты для простых приветствий.

ОТВЕТ:"""
        
        output = replicate.run(
            "meta/meta-llama-3-70b-instruct",
            input={
                "prompt": full_prompt,
                "max_tokens": 800,
                "temperature": 0.7,
                "top_p": 0.9
            }
        )
        
        # Обрабатываем ответ
        result = ""
        if hasattr(output, '__iter__') and not isinstance(output, str):
            for chunk in output:
                if isinstance(chunk, str):
                    result += chunk
                else:
                    result += str(chunk)
        elif isinstance(output, str):
            result = output
        else:
            result = str(output)
        
        result = result.strip()
        print(f"   Ответ AI: '{result[:150]}...'")
        
        return result if result else "Здравствуйте! Клиника GLADIS, меня зовут Александра. Чем могу вам помочь?"
            
    except Exception as e:
        print(f"❌ Ошибка AI: {str(e)}")
        return "Здравствуйте! Клиника GLADIS, меня зовут Александра. Чем могу вам помочь?"

def extract_name_with_ai(api_key: str, message: str) -> str:
    """
    Использует AI для извлечения имени из сообщения.
    """
    try:
        prompt = f"""
Найди имя человека в сообщении. Верни ТОЛЬКО имя или "not_found" если имени нет.
Сообщение: "{message}"
Имя:"""

        client = replicate.Client(api_token=api_key)
        
        output = client.run(
            "meta/meta-llama-3-70b-instruct",
            input={
                "prompt": prompt,
                "max_tokens": 20,
                "temperature": 0.1,
                "top_p": 0.9,
                "stop_sequences": ["\n", ".", ","]
            }
        )
        
        # Обрабатываем ответ
        result = ""
        if hasattr(output, '__iter__') and not isinstance(output, str):
            for chunk in output:
                if isinstance(chunk, str):
                    result += chunk
                else:
                    result += str(chunk)
        elif isinstance(output, str):
            result = output
        else:
            result = str(output)
        
        result = result.strip().lower()
        
        print(f"🔍 AI поиск имени из '{message}': получил '{result}'")
        
        # Очищаем ответ
        if result in ['not_found', 'none', 'null', 'нет', 'no name', '']:
            return None
        
        # Удаляем кавычки и лишние символы
        result = re.sub(r'["\'\.,!?]', '', result).strip()
        
        if not result:
            return None
        
        # Капитализируем первую букву
        if '-' in result:
            parts = result.split('-')
            result = '-'.join([part.capitalize() for part in parts])
        else:
            result = result.capitalize()
        
        # Проверяем что это похоже на имя (только буквы и дефисы)
        if not re.match(r'^[А-ЯЁа-яё\-]+$', result):
            return None
        
        return result if result else None
            
    except Exception as e:
        print(f"❌ Ошибка AI при извлечении имени: {str(e)}")
        return None

def check_interesting_application(text: str):
    """
    Проверяем, является ли сообщение заявкой на процедуру.
    """
    t = text.lower()
    
    # Ключевые слова для определения заявки
    procedure_keywords = [
        "записаться", "запись", "записать", "хочу на", "можно записаться",
        "запишите", "запишите меня", "запишите на", "запись на",
        "сколько стоит", "цена", "стоимость", "прайс",
        "эпиляция", "лазерная", "ботокс", "ботулин", "чистка",
        "пилинг", "омоложение", "лифтинг", "smas", "морфиус",
        "биоревитализация", "инъекция", "укол", "гиалуроновая",
        "консультация", "врач", "косметолог", "прием",
        "брови", "ресницы", "ламинирование", "наращивание",
        "тату", "татуировка", "удаление", "перманент",
        "массаж", "микротоки", "мезотерапия"
    ]
    
    # Если есть слова "имя" или "телефон" - это точно заявка
    contact_keywords = ["имя", "зовут", "телефон", "телефоне", "номер", "позвонить", "мне зовут"]
    
    has_procedure = any(keyword in t for keyword in procedure_keywords)
    has_contacts = any(keyword in t for keyword in contact_keywords)
    
    print(f"🔍 Проверка заявки: '{text[:50]}...'")
    print(f"   Процедурные слова: {has_procedure}")
    print(f"   Контактные слова: {has_contacts}")
    
    # Заявкой считаем если:
    # 1. Есть слова о процедурах/ценах ИЛИ
    # 2. Клиент явно указывает контакты
    return has_procedure or has_contacts
